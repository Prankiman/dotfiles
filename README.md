# DOTFILES for user configuration

## REQUIREMENTS:
    * sudo command. If not found download either sudo ``` pacman -S sudo ``` or doas ``` pacman -S doas ```. If using doas make sudo an alias.
    * base-devel installed

### SETUP:
    To install packages and setup dotfiles and config files run ``` sh setup.sh ```
